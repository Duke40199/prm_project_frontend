import 'package:flutter/material.dart';

class Destination {
  Destination(this.title, this.iconData, this.component);
  final String title;
  final IconData iconData;
  final Widget component;
}

